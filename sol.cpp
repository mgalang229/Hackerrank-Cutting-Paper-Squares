#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

ll n, m;

void decode() {
	cin >> n >> m;
	cout << (n * m - 1) << "\n";
}
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	decode();
	return 0;
}
